@host = http://localhost:3000

### Get all users
GET {{host}}/users

### Get specific user
GET {{host}}/users/2

### Get all playlists
GET {{host}}/playlists

### Get all tracks
GET {{host}}/tracks

### Get specific playlist/ GET /playlists/:id`
/ GET {{host}}/playlists/3

### Get tracks by id
/ GET {{host}}/tracks/4

### Post a playlist
POST {{host}}/playlists
Content-Type: application/json

{
  "name": "Hungry Melody",
  "description": "Beautiful song",
  "ownerId": 6,
  "tracks": 12
}






# `/playlists` router

# - `POST /playlists` creates a new playlist
#   - the request should indicate the owner of the playlist and the tracks in the playlist

# `/tracks` router

# - `GET /tracks` sends array of all tracks
# - `GET /tracks/:id` sends specific track